Yes, it is a valid function declaration. If the function is declared in this way and no arguments are passed, then the function accepts the default value, i.e. '*' in case of character and 45 in case of integer. If arguments are passed, then those values are passed and the default values are overridden.
